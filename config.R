# Configuration for DB connection
dbName = "cov19col"
hostName = "127.0.0.1"
userName = "root"
password = ""

# Logging
logDir = '.' # Path of log directory for app's custom logging. '.' = inside geomap or other module directory.
